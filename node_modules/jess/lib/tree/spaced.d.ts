import { LocationInfo, Node, NodeMap } from './node';
import { Expression } from './expression';
import type { Context } from '../context';
import { OutputCollector } from '../output';
/**
 * A space-separated expression.
 * This is basically API sugar for expressions
 */
export declare class Spaced extends Expression {
    value: Node[];
    constructor(value: (string | Node)[] | NodeMap, location?: LocationInfo);
    toModule(context: Context, out: OutputCollector): void;
}
export declare const spaced: (value: NodeMap | (string | Node)[], location?: LocationInfo) => Spaced;
