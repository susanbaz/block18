import { Node, LocationInfo, NodeMap } from './node';
import type { Context } from '../context';
import type { OutputCollector } from '../output';
/**
 * A number or dimension
 */
export declare class Dimension extends Node {
    value: number;
    unit: string;
    constructor(value: number | string | NodeMap, location?: LocationInfo);
    toString(): string;
    toCSS(context: Context, out: OutputCollector): void;
    toModule(context: Context, out: OutputCollector): void;
}
export declare const dimension: (value: string | number | NodeMap, location?: LocationInfo) => Dimension;
/** alias */
export declare const num: (value: string | number | NodeMap, location?: LocationInfo) => Dimension;
