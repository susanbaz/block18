import { Node, LocationInfo } from './node';
import type { Context } from '../context';
import { OutputCollector } from '../output';
/**
 * A JS expression
 * (compile-time node)
 *
 * @example
 * $variable
 * $(func(arg1, arg2))
 */
export declare class JsExpr extends Node {
    value: string;
    post: string;
    getValue(): string;
    getVar(context: Context): void;
    toCSS(context: Context, out: OutputCollector): void;
    toModule(context: Context, out: OutputCollector): void;
}
export declare const js: (value: string, location?: LocationInfo) => JsExpr;
