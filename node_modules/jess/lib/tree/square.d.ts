import { Node, NodeMap, LocationInfo } from './node';
import type { Context } from '../context';
import type { OutputCollector } from '../output';
/**
 * An expression in square brackets
 */
export declare class Square extends Node {
    value: Node;
    toCSS(context: Context, out: OutputCollector): void;
    toModule(context: Context, out: OutputCollector): void;
}
export declare const square: (value: Node | NodeMap, location?: LocationInfo) => Square;
