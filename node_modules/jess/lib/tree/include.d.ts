import type { Context } from '../context';
import type { OutputCollector } from '../output';
import { Node, LocationInfo } from './node';
export declare class Include extends Node {
    value: any;
    constructor(value: any, location?: LocationInfo);
    eval(context: Context): any;
    toModule(context: Context, out: OutputCollector): void;
}
export declare const include: (value: any, location?: LocationInfo) => Include;
